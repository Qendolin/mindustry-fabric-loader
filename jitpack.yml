# Note: The jitpack path is case-sensitive.
# So com/github/qendolin/mindustry-fabric-loader and com/github/Qendolin/mindustry-fabric-loader are not the same!

jdk:
  - openjdk25
before_install:
  - sdk install java 25.0.2-open
  - sdk use java 25.0.2-open